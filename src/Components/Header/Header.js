import React from 'react';
import Nav from '../SharedComponents/Nav/Nav';
import './Header.css'

const Header = () => {
    return (
        <div>
            <Nav/>
        </div>
    );
};

export default Header;